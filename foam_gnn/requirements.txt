# Base pipeline (Modules 1-3: image -> tracking -> graph).
# Versions verified during development (Python 3.11). Mirrors pyproject [project.dependencies].
# For the model/training stack (Modules 4-6) see requirements-colab.txt.
numpy==2.4.6
scipy==1.17.1
scikit-image==0.26.0
opencv-python-headless==4.13.0.92
matplotlib==3.10.9
networkx==3.6.1
pandas>=2.2,<3
scikit-learn>=1.5,<2

# dev / test
pytest>=8
