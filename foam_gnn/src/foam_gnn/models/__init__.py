"""Model backends (MODULE 4 — planned).

Will contain the spatio-temporal GNN and the three baselines (analytical
von Neumann integrator, identical-architecture vanilla GNN, per-bubble MLP),
all behind a single ``BaseEvolutionModel`` interface. Intentionally empty until
that module is implemented so that importing :mod:`foam_gnn` stays torch-free.
"""
