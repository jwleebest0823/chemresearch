# ---------------------------------------------------------------------------
# Colab-specific install (Modules 4-6: torch + PyTorch Geometric).
#
# Colab already ships a CUDA-matched torch build, so DO NOT pin/reinstall torch
# blindly — match PyG to the torch+CUDA already present. Typical Colab cells:
#
#   import torch; print(torch.__version__)          # e.g. 2.4.0+cu121
#   pip install torch_geometric==2.6.1
#   # If you need the compiled PyG companions (scatter/sparse/cluster), use the
#   # wheel index that matches your torch+CUDA, e.g.:
#   pip install pyg_lib torch_scatter torch_sparse torch_cluster \
#       -f https://data.pyg.org/whl/torch-2.4.0+cu121.html
#
# The base data pipeline (requirements.txt) does NOT need any of this.
# ---------------------------------------------------------------------------
torch_geometric==2.6.1
